# __APP_NAME__ — Development Reference

> This file is auto-generated by BioForge during repository provisioning.
> It provides context for AI coding agents (Claude Code, SkyBridge) working on this project.

## Project Overview

**Application:** __APP_NAME__
**Hostname:** __APP_HOSTNAME__
**Auth:** Amazon Cognito (Midway federation)
**Account:** __AWS_ACCOUNT_ID__

## Architecture

<!-- BioForge will inject the solution design here -->

## Knowledge Base Summary

<!-- BioForge will inject KB entries here -->

## Technical Stack

- **Frontend:** React 18 + Vite + TypeScript
- **Backend:** FastAPI (Python 3.11) on ECS Fargate
- **Routing:** ALB with CloudFront `/api/*` behavior
- **Auth:** Cognito JWT (validated in FastAPI middleware)
- **Database:** DynamoDB (on-demand)
- **Hosting:** S3 + CloudFront (frontend), ECS Fargate (backend)

## Deployment

- **Domain:** https://__APP_HOSTNAME__
- **CI/CD:** GitHub Actions (`.github/workflows/deploy.yml`)
- **Infrastructure:** Terraform (`infrastructure/`)

## Conventions

- API routes: `/api/{resource}` (FastAPI router prefix)
- Auth: `Depends(get_current_user)` on all protected routes
- CSS: v2 design tokens (Inter font, CSS variables)
- Config: `.env.production` with `VITE_*` prefix for frontend env vars

## Sprint Plan

<!-- BioForge will inject sprint tasks here if available -->
